<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/styles/profile.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        img {
            max-width: 250px;
            max-height: 250px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        header h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #333;
        }

        .profile-container {
            display: flex;
            justify-content: space-between;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            gap: 20px;
        }

        .profile-details,
        .profile-actions {
            flex: 1;
            padding: 20px;
        }

        .profile-details p {
            margin: 10px 0;
            color: #666;
        }

        .profile-actions h2 {
            margin-bottom: 20px;
            color: #333;
        }

        .profile-actions form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .profile-actions form label {
            font-weight: bold;
            color: #333;
        }

        .profile-actions form input[type="text"],
        .profile-actions form textarea,
        .profile-actions form input[type="file"] {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            box-sizing: border-box;
            font-size: 14px;
        }

        .profile-actions form textarea {
            resize: vertical;
            min-height: 100px;
        }

        .profile-actions form button[type="submit"] {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        .profile-actions form button[type="submit"]:hover {
            background-color: #0056b3;
        }

        .profile-actions form input[type="file"] {
            padding: 5px;
        }

        .profile-actions form input[type="file"]::file-selector-button {
            padding: 8px 12px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .profile-actions form input[type="file"]::file-selector-button:hover {
            background-color: #0056b3;
        }

        hr {
            border: 0;
            height: 1px;
            background: #ccc;
            margin: 20px 0;
        }
    </style>
</head>

<body>
    {{> masterHeader}}
    <div class="container">
        <header>
            <h1>Ваш профиль, {{master.Name}}</h1>
        </header>
        <div class="profile-container">
            <div class="profile-details">
                <div class="profile-photo">
                    {{#if master.Photo}}
                    <img src="data:{{master.PhotoType}};base64,{{master.Photo}}" alt="Profile Photo of {{master.Name}}">
                    {{else}}
                    <img src="/images/default-profile.png" alt="Default Profile Photo">
                    {{/if}}
                </div>
                <p><strong>Master ID:</strong> {{master.MasterId}}</p>
                <p><strong>Ваше имя:</strong> {{master.Name}}</p>
                <p><strong>Ваш логин от аккаунта:</strong> {{master.Login}}</p>
                {{#if master.Description}}
                <p><strong>Описание :</strong> {{master.Description}}</p>
                {{else}}
                <p><strong>Описание:</strong> Пока нет описания, необходимо добавить!</p>
                {{/if}}

                {{#if master.PriceForHour}}
                <p><strong>Цена за час вашей работы (BYN):</strong> {{master.PriceForHour}}</p>
                {{else}}
                <p><strong>Цена за час вашей работы (BYN):</strong> Пока нет цены, необходимо добавить!</p>
                {{/if}}
            </div>
            <div class="profile-actions">
                <h2>Редактировать профиль</h2>
                <form action="/profile/editProfile" method="POST">
                    <label for="name">Имя:</label>
                    <input type="text" id="name" name="name" value="{{master.Name}}" required>

                    <label for="description">Описание:</label>
                    <textarea id="description" name="description">{{master.Description}}</textarea>

                    <label for="price">Цена за час (BYN):</label>
                    <input type="text" id="price" name="price" value="{{master.PriceForHour}}" required
                        pattern="\d+(\.\d{1,2})?" title="Введите числовое значение (например, 1000 или 1000.50)">

                    <button type="submit">Сохранить изменения</button>
                </form>
                <hr>
                <h2>Загрузить новую фотографию профиля</h2>
                <form action="/profile/upload-photo" method="POST" enctype="multipart/form-data"
                    onsubmit="return validateFileSize(this.photo)">
                    <label for="photo">Выберите файл:</label>
                    <input type="file" name="photo" accept="image/png, image/jpeg" required>
                    <br>
                    <br>
                    <button type="submit">Загрузить</button>
                </form>
            </div>
        </div>
    </div>
</body>

</html>